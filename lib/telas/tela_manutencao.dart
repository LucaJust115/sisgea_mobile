import 'package:flutter/material.dart';

class TelaManutencao extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manutencao')),
      body: Center(child: Text('Tela de Manutencao em desenvolvimento')),
    );
  }
}
