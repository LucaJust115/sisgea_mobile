import 'package:flutter/material.dart';

class TelaInstrutor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Instrutor')),
      body: Center(child: Text('Tela de Instrutor em desenvolvimento')),
    );
  }
}
