import 'package:flutter/material.dart';

class TelaDiario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Diario')),
      body: Center(child: Text('Tela de Diario em desenvolvimento')),
    );
  }
}
